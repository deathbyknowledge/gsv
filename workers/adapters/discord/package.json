{
  "name": "@gsv/channel-discord",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "deploy:shared": "wrangler deploy --config wrangler.shared.jsonc --minify",
    "test": "vitest run",
    "test:shared": "vitest run --config vitest.shared.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260814.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.123.0"
  }
}
