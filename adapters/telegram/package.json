{
  "name": "@gsv/channel-telegram",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:managed": "wrangler dev --config wrangler.managed.jsonc",
    "deploy": "wrangler deploy --minify",
    "deploy:managed": "wrangler deploy --config wrangler.managed.jsonc --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "marked": "^16.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260804.1",
    "typescript": "^5.7.3",
    "wrangler": "4.115.0"
  }
}
