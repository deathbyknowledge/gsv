import { FsReadArgs } from "../syscalls/read";

/* What the R2 filesystem hierarchy should look like:
  / -> root
  /home/<user>/ -> user home directory (default worksapce)
  /etc/ config files?
  /
*/



