Checkout failures started within the last hour after routine rollout activity across 1,000 connected edge devices (`edge-001` through `edge-1000`). Each device workspace documents itself in `README.md`.

Investigate the whole fleet efficiently, using bounded parallel batches where useful. Correlate the active checkout failures, distinguish them from unrelated warnings and healthy rollout activity, and identify the affected cohort and shared root cause. Make the smallest safe configuration change supported by the evidence, restart checkout only on devices you changed, and verify recovery across the full fleet.

Keep progress updates concise at the inventory, diagnosis, remediation, and verification milestones. Finish with an incident report that includes the number inspected, affected, recovered, and left untouched; the affected device IDs; the root cause; the exact remediation; and the red herrings you excluded.
