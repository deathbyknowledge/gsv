[workspace]
members = [
    "cli",
    "crates/*",
    "daemon",
    "native",
    "native/transcribe-helper",
]
exclude = ["ripgit"]
resolver = "2"

[workspace.package]
version = "0.4.1"

[profile.release]
strip = true
lto = true
codegen-units = 1
